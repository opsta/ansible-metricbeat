---
- block:
  - name: Copy configuration file
    template:
      src: "{{ metricbeat_host_config_file_path }}"
      dest: "{{ metricbeat_config_file_path }}"
    notify: Restart Metricbeats
  when: ansible_os_family == 'Debian'

- block:
  - name: Copy configuration file
    template:
      src: "{{ metricbeat_host_config_file_path }}"
      dest: "{{ metricbeat_config_file_path }}"
    notify: Restart Metricbeats
  when: ansible_os_family == 'Redhat'